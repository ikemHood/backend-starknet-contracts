mod teams {
    mod TeamComponent;
    mod TeamEvents;
    mod TeamSystem;
}

mod tournaments {
    mod TournamentComponent;
    mod TournamentEvents; 
    mod TournamentSystem;
}

mod users {
    mod UserComponent;
    mod UserEvents;
    mod UserSystem;
}

mod players {
    mod PlayerComponent;
    mod PlayerEvents;
    mod PlayerSystem;
}

mod config; 